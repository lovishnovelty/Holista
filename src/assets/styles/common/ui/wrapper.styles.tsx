import {StyleSheet} from 'react-native';
import {color} from '../../../theme';

export const wrapperStyle = StyleSheet.create({
  container: {flex: 1, backgroundColor: color.white},
  child: {backgroundColor: color.white, flex: 1},
});
