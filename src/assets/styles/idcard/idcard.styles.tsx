import {StyleSheet} from 'react-native';
import {normalize} from '../../../utils';

export const idCardStyle = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: normalize(20),
    marginBottom: normalize(20),
  },
});
